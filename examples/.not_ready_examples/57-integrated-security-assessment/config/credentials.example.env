# 外部工具 API/认证示例配置
# 复制为 credentials.env 或写入当前 shell 环境使用

# === Gophish ===
# 若未在 config.json 中设置 api_key，可在运行控制台前导出此变量
# GOPHISH_API_KEY=changeme

# === OpenBAS ===
# 登录 OpenBAS 后在设置中生成访问令牌
# OPENBAS_TOKEN=changeme
# OPENBAS_BASE_URL=https://localhost:8443

# === PentestAgent ===
# 主要环境变量需写入 external_tools/pentest-agent/.env，可参考该项目内 .env.example
