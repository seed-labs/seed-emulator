id: Z02
slug: Z02_a02_mpls_control_plane
title: A02 MPLS / FRR Control-Plane Inspection
source_examples:
  - family: A
    example_id: A02_transit_as_mpls
    output_dir: examples/basic/A02_transit_as_mpls/output
runtime_contract:
  stage: already_running_network
  planner_visibility: runtime_restricted
  full_container_access: true
  future_partial_visibility_ready: true
tool_contract:
  high_level:
    - seed_agent_run
    - seed_agent_plan
    - seed_agent_policy_check
  preferred_ops:
    - workspace_refresh
    - inventory_list_nodes
    - routing_protocol_summary
    - routing_looking_glass
    - ops_exec
    - job_get
    - job_steps_list
  risky_ops: []
  shell_command_policy:
    default: narrow_control_plane_inspection_allowed
    allowed_only_for:
      - explicit FRR and MPLS evidence through vtysh
      - read-only daemon and config inspection
orchestration_contract:
  canonical_loop:
    - attach
    - inspect inventory
    - summarize routing backend
    - inspect FRR/MPLS evidence
    - summarize boundaries
  primary_entry: seed_agent_run
  structured_task_entry: []
  deterministic_fallback: []
skill_contract:
  task_skill_patterns:
    - attach-and-scan
    - control-plane-inspection
    - evidence-collection
  why_it_fits:
    - A02 is the best current example for mixed BIRD and FRR behavior.
    - It validates the protocol-aware routing toolchain.
command_contract:
  interactive_ui: ./scripts/seed-codex ui -m gpt-5.4 -c 'model_reasoning_effort="low"'
  read_only_probe: ./scripts/seed-codex run "Attach to examples/basic/A02_transit_as_mpls/output; summarize routing backend behavior and identify where explicit FRR evidence is required." --workspace-name z02_probe --attach-output-dir examples/basic/A02_transit_as_mpls/output --policy read_only
  explicit_control_plane_note:
    - "Use routing_protocol_summary(auto) first."
    - "Use ops_exec with vtysh only for FRR/MPLS evidence, not as the default path."
status:
  live_readiness: conditional_go
  best_for:
    - protocol-aware routing validation
    - FRR/MPLS inspection
    - toolchain regression checks
  hard_boundaries:
    - This is a control-plane validation bundle, not the best end-user task story.
    - "backend=frr returning bgpd is not running is a real negative signal on MPLS core nodes."
