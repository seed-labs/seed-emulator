# Instructions

- Step 1: Run `vpn_connect.sh` to connect this VM to the remote emulator. The command should provide the IP address of the remote machine

- Step 2: Run `client_setup.sh` to et up the client side, including setting up the routing and `/etc/hosts`. The attack target is included in the `target_networks_host` file. 

