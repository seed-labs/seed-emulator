# Demo Instructions

1. VPN into the emulator on another machine: run `vpn_setup.sh`
2. Set up the routing table: run `route_setup.sh`
3. Set up the /etc/hosts (fix the IP address of the target):
   run `dns_setup.sh` 


